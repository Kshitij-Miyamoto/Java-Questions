public class return1 {
    static int sum(int x){
        return (5 + x);
    }
    //here we make use of return values, and understand how can we use return values
    public static void main(String[] args) {
        System.out.println(sum(12));
    }
}
